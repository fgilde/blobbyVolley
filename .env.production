# Relay server the production (GitHub Pages) build connects to.
# This is the public Render URL from render.yaml (service name -> subdomain).
# Override at runtime without rebuilding via ?relay=wss://... in the URL.
VITE_RELAY_URL=wss://blobbyvolley-relay-fgilde.onrender.com
